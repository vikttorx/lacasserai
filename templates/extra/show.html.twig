{% extends 'base.html.twig' %}

{% block title %}Extra{% endblock %}

{% block body %}
    <h1>Extra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ extra.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ extra.Description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ extra.Image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('extra_index') }}">back to list</a>

    <a href="{{ path('extra_edit', {'id': extra.id}) }}">edit</a>

    {{ include('extra/_delete_form.html.twig') }}
{% endblock %}
